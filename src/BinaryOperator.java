public enum BinaryOperator {

    // done
    PLUS, MUL, MINUS,
    // done
    AND, OR,
    // done
    DIV, MOD,
    // done
    EQUAL, UNEQUAL,
    // done
    LEQ, GEQ,
    // done
    LESS, GREATER
}
